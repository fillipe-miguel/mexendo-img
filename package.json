{
	"name": "resize-image",
	"version": "1.0.0",
	"description": "Aplicativo node para comprimir imagens",
	"main": "app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"sharp": "^0.30.2"
	}
}
